# CLAUDE.md — Frontend Website Rules (Mobile App prototype)

## Always Do First
- If a `frontend-design` skill is available in the session, invoke it before writing frontend code. Otherwise apply the Anti-Generic Guardrails below.

## Reference Images
- If a reference image is provided: match layout, spacing, typography, and color exactly. Swap in placeholder content (images via `https://placehold.co/`, generic copy). Do not improve or add to the design.
- If no reference image: design from scratch with high craft (see guardrails below).
- Screenshot your output, compare against reference, fix mismatches, re-screenshot. Do at least 2 comparison rounds. Stop only when no visible differences remain or user says so.

## Local Server
- **Always serve on localhost** — never screenshot a `file:///` URL.
- Start the dev server: `node serve.mjs 3001` (serves the project root at `http://localhost:3001`)
- Port 3000 is NOT ours — another app (an "Agile Pharmas" dev server) already listens there. This project uses **3001**.
- `serve.mjs` lives in the project root; it takes the port as its first argument (default 3000). Start it in the background before taking any screenshots.
- If the server is already running, do not start a second instance.

## Screenshot Workflow
- Puppeteer is installed locally in this project (`node_modules/`). Its Chrome browser cache lives at `C:\src\puppeteer-cache` (set via `.puppeteerrc.cjs`) — an ASCII-only path, because the user profile folder contains Arabic characters that can break Chrome tooling.
- **Always screenshot from localhost:** `node screenshot.mjs http://localhost:3001`
- Screenshots are saved automatically to `./temporary screenshots/screenshot-N.png` (auto-incremented, never overwritten).
- Optional label suffix: `node screenshot.mjs http://localhost:3000 label` → saves as `screenshot-N-label.png`
- `screenshot.mjs` lives in the project root. Use it as-is.
- After screenshotting, read the PNG from `temporary screenshots/` with the Read tool to see and analyze the image directly.
- When comparing, be specific: "heading is 32px but reference shows ~24px", "card gap is 16px but should be 24px"
- Check: spacing/padding, font size/weight/line-height, colors (exact hex), alignment, border-radius, shadows, image sizing

## Output Defaults
- Single `index.html` file, all styles inline, unless user says otherwise
- Tailwind CSS via CDN is the default for NEW pages: `<script src="https://cdn.tailwindcss.com"></script>`
- **Exception — the current `index.html`:** it uses hand-written inline CSS (no Tailwind) because it is also published as a claude.ai artifact, whose CSP blocks all CDN scripts. Keep it CDN-free unless the artifact is abandoned.
- Placeholder images: `https://placehold.co/WIDTHxHEIGHT`
- Mobile-first responsive

## Brand Assets
- Always check the `Assets/` folder before designing (this project's asset folder; the generic rule says `brand_assets/`). It may contain logos, color guides, style guides, or images.
- The quick-action icons in `Assets/` are glossy orbs on black backgrounds — run `node process-icons.mjs` to circle-crop and inline them into `index.html` (outputs also saved to `Assets/processed/`).
- If assets exist there, use them. Do not use placeholders where real assets are available.
- If a logo is present, use it. If a color palette is defined, use those exact values — do not invent brand colors.

## Anti-Generic Guardrails
- **Colors:** Never use default Tailwind palette (indigo-500, blue-600, etc.). Pick a custom brand color and derive from it.
- **Shadows:** Never use flat `shadow-md`. Use layered, color-tinted shadows with low opacity.
- **Typography:** Never use the same font for headings and body. Pair a display/serif with a clean sans. Apply tight tracking (`-0.03em`) on large headings, generous line-height (`1.7`) on body.
- **Gradients:** Layer multiple radial gradients. Add grain/texture via SVG noise filter for depth.
- **Animations:** Only animate `transform` and `opacity`. Never `transition-all`. Use spring-style easing.
- **Interactive states:** Every clickable element needs hover, focus-visible, and active states. No exceptions.
- **Images:** Add a gradient overlay (`bg-gradient-to-t from-black/60`) and a color treatment layer with `mix-blend-multiply`.
- **Spacing:** Use intentional, consistent spacing tokens — not random Tailwind steps.
- **Depth:** Surfaces should have a layering system (base → elevated → floating), not all sit at the same z-plane.

## Hard Rules
- Do not add sections, features, or content not in the reference
- Do not "improve" a reference design — match it
- Do not stop after one screenshot pass
- Do not use `transition-all`
- Do not use default Tailwind blue/indigo as primary color

## MedsCheck Pre-flight
- The MedsCheck session opens with 5 guided micro-steps (Modality → Provider → Reason → Caregiver → Lifestyle) inside the Pre-flight top-level tab; state lives in the in-memory `PRE` object keyed by patient id, so back navigation and interruptions never lose entered data.
- Reason step enforces the real minimum of 3 reasons (auto-detected from record data: 3+ meds, 2+ conditions, 2+ prescribers) with inline amber messaging and a disabled Next.
- Modality ("Phone") is stored in `PRE[pid].modality` and drives the Acknowledgement tab (built): In-person → patient signs; Phone → "Conducted by phone with [name] on [date]" + pharmacist signs. Signature stored as data URL in `PRE[pid].ack`; empty signature disables Confirm (error prevention).
- The canvas signature pad is THE reusable signature component: `attachSigPad(canvas, hint, initialDataUrl, onChange)` returns `{inked, dataUrl, clear, load}`. Reuse it for the Worksheet's Personal Medication Record instead of building a new one.
- Acknowledgement has "Sign in full screen" (`openSigFullscreen`): a landscape overlay (`.sig-full` > `.sig-rot`, rotated 90°) where the whole phone is the pad; `attachSigPad` detects `.sig-rot` and remaps pointer coords. Done → the signature flows back into the inline pad (aspect-fit) and enables Confirm.
- Notify tab (built): "No action" (default, neutral filing line) / "Follow-up action" (textarea + violet "Draft with AI" chip composing from `PRE[pid].worksheet.flags` when the Worksheet exists, else pre-flight reasons; blue info banner names the Pre-flight provider; fax is simulated). Complete requires a non-empty note when follow-up is chosen.
- "Complete MedsCheck →" pushes a claim {service, $60, patient, status: "ready"} onto the in-app `BILLING` array and navigates to the Billing screen (`renderBilling`) — this project's equivalent of the other app's `billing.html`. The home Billing tile opens the same screen.
- Informational blue (#e8f1fd/#2f6fd0) and AI violet (#f1edfc/#7c5cd6) are now instantiated per behavioral_ux_design_director.md §11 — use only for info banners / AI-drafted content respectively.

## Color System (current — lavender/peach, 2026-08-12)
- The teal system was replaced wholesale to match a lavender reference. Locked tokens: primary accent `#8B7CF6`; accent tints `#EEEBFC` (fill) / `#E4DFFA` (hover) / `#CFC7F5` (chip border) / `#F1EEFB` (patient header); text `#2B2A44` (heading) / `#3A3956` / `#6E6C88` (secondary) / `#8A88A3` (muted) / `#C4C1D9` (chevrons); borders `#ECEAF5` / `#DDD9F0`; ground `#F6F5FA`; tile fill `#FDFCFF`; tinted shadows `rgba(58, 50, 110, α)`.
- Home hero is a **light** gradient — peach `#F6DCCB` → `#EFDAD8` → lilac `#E3D8F3` → `#D9D3F7` — with **dark** text and semi-opaque white glass; the toggle thumb is solid accent with white label.
- Semantic colors unchanged: green `#1db871`, red `#e0403d`/`#fdecec`, amber `#f6a723`/`#fdf0dc`, blue info, AI violet.
- Deep links for testing: `#session-medscheck-<pid>-s<1-5>`, `-ack` (in-person), `-ackphone` (phone variant), `-notify`.
- Worksheet step 1 (built): `renderMedReview` — ONE medication per screen with a per-med progress bar ("Medication N of total"), title (drug + dose from `DOSE_KB`), editable Indication auto-filled from `MED_KB`, read-only Target only for classes that have one (`CLASS_TARGET`: BP 140/80 for ACEi/CCB/diuretic/β-blocker, HbA1c < 6.5 for antidiabetics, LDL < 2 · TC < 5 for statins), Adherence segmented (As prescribed / Issue → inline note + violet AI-draft + Save/Edit), optional Side effects (+ Add → text + per-class quick-picks `CLASS_SE` + required Mild/Moderate/Severe; Mild neutral, Moderate amber, Severe red). State in `PRE[pid].worksheet.meds[drug]`; `syncWsFlags` rebuilds `worksheet.flags` (adherence issues + side effects), which the Notify AI draft consumes. Last med's button reads "Continue →" → `renderWsStep2Stub` (Interactions — next build step, bridges to Notify).
- **Correction 1** — `comboWarnings(p)` (≥2 BP-class drugs → "Multiple blood-pressure medications — low blood-pressure risk"; ≥2 sedating → overlapping-sedation) render as amber `.warn-card` banners at the very top of medication 1 only, each with its confirmation checkbox (`worksheet.comboAck`). Nothing renders when none apply. Step 1 also has "+ Add OTC / natural product the patient takes" on the last medication (`worksheet.otc`), the side-effect field is labelled "Side effect / observation" with quick-picks + violet "✨ Plain-language" chip + free text + severity, and the adherence note has Dictate + AI draft.
- **Step 2 Interactions** (`renderInteractions`, **Correction 2**): always shown, title + "Detected automatically among the medications under review." `IX_PAIRS` → High (red) / Moderate (amber, checkbox required before Next) / Minor (neutral, collapsed behind "Show details"). None found → neutral `.reassure` line "No drug-drug interactions detected among the reviewed medications." Never skips.
- **Step 3 Drug-therapy problems** (`renderDTP`, **Correction 3**): `dtpSuggestions(p)` cards ("SUGGESTED · …", rationale, "✦ AI-drafted rationale — review before logging" with the ✦ portion in AI violet) with exact buttons **"Accept & log"** (solid amber `.btn-amber`) / **"Dismiss"** (`.btn-outline`). Logged list + "Quick checks before logging": OTC medicines taken? · Herbal/natural products used? · Free samples from another prescriber? · Date of last antibiotic use (+ N/A) · Flu vaccine this season? · COVID-19 vaccine received? (`worksheet.quick`).
- **Step 4 Personal Medication Record** (`renderPMR`, **Correction 4**): per-med Indication / How it works / How to take it (`MED_HOW`), OTC list, "Pharmacist recommendations" with **Dictate** (left) + **AI draft** (right), pharmacist signature via `attachSigPad` (+ full screen), confirmation checkbox; Continue gated on saved rec + signature + confirmation → Notify.
- **Dictate** = `attachDictate(btn, textarea, onText)` using the Web Speech API (`webkitSpeechRecognition`, en-CA) — live in Chrome/Edge; in unsupported browsers the button is disabled and reads "Dictate · soon". Appends to the field.
- Deep links: `-meds`, `-ix`, `-dtp`, `-pmr`.
- The 7 desktop reference screenshots referenced by the alignment prompt were NOT attached in this project — wording was implemented from the prompt text; verify against the screenshots when available.
- `behavioral_ux_design_director.md` governs UX decisions (audit-first per its §27); `design_vision.md` and `mobile_ux_checklist.md` were referenced in the spec but are not in this project yet.

## Incident reporting (2026-08-24 — OCP AIMS, from desktop reference screenshots)
- **Log** `#scrIncidents` (`renderIncidents(ret)`, deep link `#incidents`; `incReturn` remembers Home/More/Notifications for Back; More tab lit). Header + `.pd-link` "NIDR sync" (flips `nidr:"pending"`→`"submitted"`, one-shot flash; simulated). Stats: 2 `.sum-tile`s — events last 90 days (+% near misses) and open workflow items (amber `.warn` when >0) — plus a note with next CQI (`INC_CQI`, quarterly) and safety self-assessment (`INC_SSA`, biennial). Event log rows = `.txn.cal-row.inc-row` (date col `.inc-dt`, wrapping title + kind pill `Incident`=`.stat-pill.sub` / `Near miss`=`.stat-pill.info` (blue) + `harm: none`, stages · meds line, right column `.inc-pills`: status `Needs analysis`/`Share learnings`=`.stat-pill.warn`, `Complete`=`.paid`, + `NIDR submitted|pending`). Fixed CTA "Record incident →". Seed data mirrors the 5 desktop rows.
- **Record form** (`renderIncForm`, via `enterSession("incidents", pid?, from)` → runs on `scrSession` in focus mode; deep links `#incident-new`, `#incident-new-<pid>`): micro-steps like Pre-flight — 1 Event type (Incident/Near miss segl + blue "Anonymous by design" banner) + date (max today) + optional time · 2 Patient context (age-range chips `INC_AGES`, gender, route — all optional, **never a name/birthdate**) · 3 Type (`.opt` single-select rows, `INC_TYPES`) + Discovered by + optional Prescribed by · 4 Stages (multi chips, required) + meds (name+DIN rows, + add, quick-pick chips from the patient's meds when entered via patient) + associated factors · 5 Degree of harm (`.opt` rows, **Incident only — Near miss = 5 steps total**) · 6 Describe & record (Dictate + **identifier guard** `incIdentifiers`: flags patient names from `PATIENTS` and ≥7-digit runs → red `.ferr` + gate "remove identifying details") + contributing factors + immediate actions. CTA "Record anonymously →" unshifts into `INCIDENTS` with status `Needs analysis` (incident) / `Share learnings` (near miss), `nidr:"pending"`, **no patient id/name stored ever** — entering via a patient only pre-fills the de-identified age band + med quick-picks (green note says so).
- **Wired everywhere**: Home **Modules view** row under Quick Actions (`#homeInc` → log) · **More hub** row with live open-count subtitle · **patient → Choose module** "Pharmacy programs" row (`#modInc`, "never linked to <first name>" → prefilled form) · **Notifications** (computed): open workflow items (amber) + CQI-due-within-7-days, both tap → log. `goBackFromSession` handles `scrIncidents`. New CSS: `.opt` rows, `.stat-pill.info/.warn`, `.inc-*`.

## Scan-first list pattern (2026-08-24 — density/scanability spec; REUSABLE GLOBALLY)
- **The rule**: a list answers only what/when/status/needs-action; everything else lives in a detail view. "Show enough to recognize and prioritize — not enough to fully understand."
- **Event Log rows** (`.inc-row`, whole row `role=button` → chevron): quiet date column · title (`-webkit-line-clamp: 2` — max 2 lines per rule 11) · ONE ellipsized metadata line with **short stage labels** (`INC_STAGE_SHORT`: "Preparation / dispensing"→"Dispensing", "Rx order entry"→"Order entry"…) + first med only ("Hydralazine +1") · `.pl-line` with ONE dominant status pill (+ `NIDR pending` ONLY when sync is actionable). Kind/harm/NIDR-submitted/descriptions moved OUT of rows. ≥4 rows fit the first viewport.
- **Event details** (`#scrIncDetail`, `renderIncDetail(i)`, row tap): the information destination — full pills row (status/kind/NIDR), **one clear action** for open items ("Mark analysis complete →" / "Mark learnings shared →" → status Complete + green flash + bell resync), kv card (Category / harm / full stages / all meds+DIN / Discovered by / de-identified patient context / NIDR hint), What happened, Contributing factors, Immediate actions. Recorded events now store desc/factors/actions/disc/context for this screen; seeds carry short de-identified descriptions.
- **Incremental loading**: `incShown = 10` + "Load more · N older" `.btn-outline` that only renders when `INCIDENTS.length > incShown` (rule 8: never for small datasets). Filters intentionally NOT added (dataset small — rule 9).
- **Apply to any long list** (activity, billing, histories, claims…): summarize in the list → detail on tap → load incrementally when long. Audited existing lists: Patients / Billing / Notifications / Calendar rows already conform (≤1 pill, 2-line rows).

## Session focus mode
- Any module session (`scrSession` — Pre-flight, Acknowledgement, Worksheet, Notify, and other modules' session pages) hides the floating bottom nav; `showScreen` toggles `.screen.focus`, which also drops the fixed CTA to `bottom: 26px`. The nav returns automatically on any non-session screen (Back, Billing handoff, etc.).
- **Bug fixed 2026-08-24**: `setPreApp(null)` (re-entering the app — e.g. unlocking the idle lock mid-session) used to force the nav visible over the session, covering the fixed CTA (user: "السيشن الخاصة بالmeds check مكنتش كدا"). It now checks the currently visible screen and keeps the nav hidden on `scrSession`/`scrBook` — same rule as `showScreen`.

## Notifications
- The home bell opens `#scrNotifs` (`renderNotifs`). Items are COMPUTED from the same data as the rest of the app (`buildNotifs`): vaccines due today, today's appointments, injections due, billing claim events, MedsCheck-eligible summary, and simulated fax confirmations from completed sessions. Grouped Today / Earlier; tapping opens the patient / Billing / filtered Patients list; "Mark all read" clears the bell's red dot (`NTF_READ` in-memory). Deep links: `#notifications`, `#billing`.

## Epic 0 — Auth & Onboarding (2026-08-16; sales-led B2B, no public sign-up)
- Pre-app overlays (`.pre`, z 15): `#onb` (3 intro slides — kept from the earlier request; ask if they should go now that accounts are provisioned) → `#auth` **0.1 Sign in** → `#setup` (**0.4 Terms** + **0.3 setup**, first sign-in only) → app. `#reset` (0.2), `#lock` (0.7) and the `#sheet` bottom sheet (0.6) are siblings. `setPreApp(state)` toggles them (`"onb"|"auth"|"reset"|"setup"|"lock"|null`); `preState_` tracks it.
- **Shared form validation** for every pre-app form: `formValidate(spec)` / `formBind(spec, onValid)` / `formRevealErrors(spec)` (spec = `{fields:[{id,err,label,rule}], gate, btn}`) — per-field `.ferr` after touch, `gateHtml` line, disabled submit, Enter handled manually. `emailRule` shared. `.pw-toggle[data-pw]` = show/hide password (delegated).
- **0.1 Sign in**: email + password (Show/Hide), "Keep me signed in", **no Sign up** (footer: accounts are set up by the AgileRx team). Simulated backend: any valid email+password succeeds; password `wrong` → invalid-credentials banner (`.auth-err`, names both fields, inline "reset your password"); email ending `@offline.test` → network banner with "Try again". 600 ms "Signing in…" state. Success → `SETUP.done ? enterApp() : showSetup()`; stores `PROFILE.email`.
- **0.2 Reset** (`RESET`, `showReset(step,email)`, `renderReset`): `request` (email → "Send reset link", 500 ms) → `sent` ("Check your email — we sent a reset link to X", Back to Sign in, Resend, and a muted "Prototype: open the reset link" that jumps to) → `new` (new + confirm password ≥8, must match) → back on Sign in with green `.auth-banner` "Password updated…" and the email prefilled. Deep links `#reset`, `#reset-sent`, `#reset-new`. Nothing is emailed.
- **0.3/0.4 Setup** (`SETUP` in `sessionStorage agilerx.setup`; `showSetup(step)`, `renderSetup`, `setupGate`; 5 steps, `.pbar` + "Step N of 5" like Pre-flight; Back keeps data): 1 **Terms & privacy** (`.terms-box` scrollable placeholder legal text + required checkbox; Continue disabled until checked) · 2 **Your pharmacy** (name prefilled from `PROFILE.pharmacy` = "sales team provided", address + phone ≥10 digits, inline errors on blur) · 3 **Connect Fillware** (explanation card, "Connect Fillware" → 1.2 s "Connecting…" → "Connected · <pharmacy> · 1,284 patient records synced"; Continue gated on connected; simulated) · 4 **Add your pharmacists** (embeds 0.5) · 5 **You're all set** (summary card, "Go to Home →" sets `SETUP.done`, copies pharmacy name into `PROFILE`). Deep links `#setup`, `#setup-<1-5>`. Step 1 has a "Sign out" link.
- **0.5 Staff** (`STAFF` in `sessionStorage agilerx.staff`; `staffOwner()` seeds the signed-in pharmacist as Active/owner; `staffHtml()` + `bindStaff(onChange)`): name + email (valid, not already on the account) → "Send invite" → row with `Invited` pill (`.stat-pill.sub`; Active = `.paid`), one-shot green "Invite sent to … (simulated)". Reachable from setup step 4 and **Settings → Team → Pharmacists** (`renderStaff`, deep link `#staff`).
- **0.6 Switch pharmacist**: Settings identity card is now `<button class="id-card">` with caption "SIGNED IN AS" + ▾ → `openSwitchSheet()` bottom sheet (`.sheet-scrim` + `.sheet`, title "Signed in as"): Active pharmacists as `.txn[role=button]` rows with initials, current row = check; tap → `PROFILE` swaps (name/creds/email; pharmacy kept), Settings re-renders — no password (shared in-store device). Invited pharmacists are not listed (note explains). Deep link `#switch`.
- **0.7 Lock**: `IDLE_MS = 5 min` (placeholder) — `idleReset()` on pointer/key/touch/wheel while inside the app; on timeout `showLock()` renders `#lock` (initials avatar, "Session timed out.", password only + Show/Hide, Unlock disabled until typed; password `wrong` → inline error; "Not <first name>? Sign out" → Sign in). Deep link `#lock`. Set `IDLE_MS = 300; idleReset()` in the console to test.
- Gate order (`gate` IIFE): hash deep-links bypass; else signed-in + `SETUP.done` → app; signed-in only → setup; onboarded → sign in; fresh → intro slides. `#signup` now just opens Sign in.

## Glanceability pass (2026-08-24 — "one clear action per screen" directive)
- The user adopted a north star: fewer decisions/taps/typing, glanceability, one clear action per screen, progressive disclosure, visible progress, patient-first context. Audit found 6 misfits; fixes 1–5 built, fix 6 (Home Patients/Modules toggle) deliberately NOT changed — user specced that layout explicitly; ask before touching.
- **DTP step 3 split** (`renderDTP`, two phases): unresolved suggestions render ONE per screen ("Suggestion N of M" note, Accept & log / Dismiss are the only actions, no Next, "N problems logged so far" line); when none remain → checks phase (logged list with remove + 6 quick checks + gated Next). Back from either phase → Interactions. `-dtp` deep link unchanged.
- **Booking slot chips** (`freeSlots(off, dur)`): first free 15-min-grid slot per hour, capped at 6 evenly spread `.chip` buttons ("9:00 AM"…), computed from `PHARMACY_HOURS` + `APPT_DURATION` + existing appointments — chips are conflict-free by construction. Shown once patient+service+valid date; hint before that; "Pick a custom time ›" toggles the old native time input (`BOOK.custom`) with full validation as fallback; no-free-slots message when the day is full.
- **Patient details one dominant CTA** (`openPatient`): only the TOP recommendation keeps the `.act-card` with a solid CTA; further recs are quiet `.txn[data-mod]` rows (icon · title · status · fee) wired to the same `enterSession`. "All caught up" fallback unchanged.
- **PMR progressive disclosure** (`renderPMR`): the auto-filled med record is collapsed to a `#pmrToggle` summary row ("Medication record · 6 medications + N OTC · auto-filled · tap to review", `aria-expanded`, `w.pmr.recOpen`); actions (recommendations/signature/confirm) sit in the first viewport.
- **Fresh visits go straight to Sign in** — the gate's fallback is `goAuth()`; the 3 intro slides survive ONLY at `#onboarding`.

## More hub + Settings (2026-08-16)
- The **More** tab opens `#scrMore` (`renderMore`) — a MINIMAL hub built only so Settings is reachable (there was no More hub in this prototype before): rows for existing destinations Billing, Notifications, Settings. Do not expand it without a request.
- **Settings** (`#scrSettings`, `renderSettings`, deep link `#settings`; `#more` for the hub): back button (aria "Back to More") + "Settings" title per the standard nested header (`section-head left` + `filter-btn` + `screen-title`); identity card (initials `.id-ava` 56px, "Name, RPh", pharmacy, "Edit profile ›" row → `renderEditProfile` placeholder); **AI draft style** segmented `Short & numeric | Detailed` (individual-level only — no organization-level control here by design); **Appearance** segmented `Light | Dark | System` (default System); neutral outlined **Sign out** (`.btn-outline`) → clears `agilerx.signedin`, returns to Sign in.
- Preferences persist in `localStorage agilerx.settings` (`SETTINGS = {aiStyle, theme}`); read via `aiStyle()`. AI generation (`recDraft`, `draftNote`, adherence/side-effect drafts) does NOT consume `aiStyle()` yet — wire it when asked.
- Appearance applies `data-theme="light|dark"` on `<html>` (`applyTheme`; System removes the attribute → `prefers-color-scheme`). Only the page backdrop around the phone is theme-aware; **the phone UI has no dark palette yet** — hardcoded hex throughout. Flagged, not built.
- `TAB_OF` in `showScreen` maps screens → highlighted nav tab (Home 0, Calendar 1, More/Settings 3, everything else Patients 2 — unchanged behavior for Billing/Notifications).

## Calendar (2026-08-16)
- Calendar tab → `#scrCalendar` (`renderCalendar`; deep links `#calendar`, `#calendar-<dayOffset>` e.g. `#calendar-12`). Header "Calendar" (no back) + a `.pd-link` "Today" that appears only when not on today. **Month grid** (`.cal-month` card: ‹ Month YYYY › with `filter-btn` arrows, S–S weekday row, 7-col `.cal-grid` of 46px `.cal-cell` buttons; other-month days muted & inert; **today** = accent text + `#CFC7F5` inset ring; **selected** = filled accent circle; 5px accent dot under days with appointments). Beneath: the selected day's appointments — heading reuses `.ntf-group` ("Today · Sun, Aug 16 · 3 appointments" / "Tomorrow · …" / "Yesterday · …"), rows reuse `.txn[role=button]` with a 68px time column (`.cal-row .tm`, 12-hour via `fmt12`) + name + "Service · N min" + chevron. State: `calSel` (Date), `calMonth` (first of shown month) — survive Back from a session. The first build used a Today/This-week segmented list; user said "I WANT AN ACTUAL CALENDAR VIEW" → replaced with the grid (2026-08-16).
- **Data**: today's appointments come from the existing per-patient `appointmentToday` field (single source shared with status lines/notifications); service inferred by `apptService(p)` (MedsCheck if eligible, else Minor Ailments if an MA condition applies, else "Visit" with no duration). Other days live in `WEEK_APPTS` (`{pid, dayOffset, time, service}` — days +1..+12), merged by `appointments()`; `dayKey(date)` buckets them for the grid.
- **Durations** — `APPT_DURATION = { medscheck: 30, ailments: 15 }` are the ONLY documented values. Vaccination/Injection appointment durations are undefined → such rows render without "· N min". Do not invent one; ask.
- Tapping a row calls the SAME `enterSession(mod, pid, "scrCalendar")` entry point (resumes at the saved step); `goBackFromSession` returns to Calendar and keeps the chosen view. The generic session Back button now also uses `goBackFromSession` (was a duplicated branch).
- Empty state: "No more appointments today." (today) / "No appointments on Sat, Aug 22." (other days) in a card — never blank.
- "+ New appointment" = fixed `.btn-primary` above the nav → `renderBooking()` (see Validations below).
- Home has no "Today's appointments" teaser in this prototype (it has Ready to Act) — nothing to link from there.

## Validations (2026-08-16 — user: "I NEED TO ADD VALIDATIONS", chose all three in order)
- **Shared pattern (§19)**: `gateHtml(items)` renders "**N items remaining** · a · b" (`.gate`, amber, `warnSm`) inside `.next-fixed` above the primary button, which is `disabled` while items remain; click handlers re-check the same predicate. Field-level errors use `.fld` / `.tinput.invalid` / `.ferr.show` (red `#e0403d`, 12px) and appear only after the field is touched (blur, or typing) — never on first paint. `.next-fixed::before` is now a general fade sized by the CTA + gate (focus mode: `bottom:-26px`; `.page.pf` focus padding-bottom 150px).
- **A. Sign in / Sign up** (`AUTH_FIELDS`, `validateAuth`, `revealAuthErrors`): name ≥2 chars + pharmacy required (sign up only), email regex, password ≥8 (sign up) / non-empty (sign in). Submit disabled until valid; gate line under "Keep me signed in" appears once any field is touched; Enter on an incomplete form (browsers skip implicit submit when the button is disabled) is handled manually → shows all errors + focuses the first. `#auth` is now scrollable. Fixed in passing: `.auth-row[hidden]` (Keep-me-signed-in showed on Sign up).
- **B. New appointment** (`#scrBook`, `renderBooking`, `bookErrors`, deep link `#book`; focus mode): patient (inline search, same `.txn` rows), service segmented (MedsCheck / Minor Ailments) with **eligibility** check via `moduleState` (ineligible → red inline text from `moduleState().text`, gate item "eligible service"; eligible → green "Service · N min · status"), native `type=date` (min today; past → error), native `type=time` step 5 min: outside `PHARMACY_HOURS` (**09:00–18:00 — ASSUMED, not documented; flag**), would end after close (uses `APPT_DURATION`), **overlap** with any existing appointment that day (interval test, names the clash "Overlaps with Lin Wei · MedsCheck 10:00 AM–10:30 AM."). Booking pushes into `WEEK_APPTS`, selects the day, shows a one-shot green `.reassure` confirmation (`calFlash`). Draft persists across Back if anything was entered (§17). Not checked: time-of-day already past for today (mock clock is 9:41).
- **C. MedsCheck session gates** — Pre-flight: step 3 ≥3 reasons ("at least 3 reasons (N selected)"), step 4 caregiver name + relationship when present (inline `.ferr` on blur, `s.cgTouched`; updated in place via `pfSync` so focus is kept), step 5 smoking / cessation-if-smoker / exercise / alcohol. Ack: patient|pharmacist signature (re-gated on Clear). Worksheet 1: unconfirmed combo warnings (med 1), Issue without saved note, open side-effect editor, open OTC editor (`mrGate`/`mrSync`). Worksheet 2: "review N interactions with the patient". Worksheet 3: "accept or dismiss N suggested problems" + quick checks (lists them when ≤2, else "answer N quick checks"; antibiotic date or N/A). PMR: recommendations (or "save the recommendations" when typed but unsaved) · signature · confirmation. Notify: "follow-up action for <provider>" when Follow-up chosen.

## Project Notes
- This project is a mobile banking app prototype (reference: fintech home screen shared 2026-08-12).
- Current deliverable: `index.html` — phone-framed home screen, published as artifact at https://claude.ai/code/artifact/5e7b7d9d-89ec-41fd-a5e6-ab2a7f75b51b
- Preview during work: VSCode Live Preview extension (auto-refreshes on save).
